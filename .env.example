# Project Configuration
# Used in Docker and CI/CD processes
PROJECT_NAME="Frontend Template"  
# Application name displayed in UI
APP_NAME="Frontend Template"      

# Port for both development and production servers
PORT=3000                         

# API Configuration (if needed)
# API_BASE_URL=http://localhost:8000/api  # Backend API base URL
# API_TIMEOUT=5000                        # API request timeout in milliseconds

# Feature Flags (if needed)
# ENABLE_ANALYTICS=false          # Enable/disable analytics tracking
# ENABLE_DEBUG=true               # Enable debug mode and logging

# Build Configuration
# BUILD_OUTDIR=dist               # Output directory for production build
# BASE_URL=/                      # Base URL for deployment (useful for subdirectories)